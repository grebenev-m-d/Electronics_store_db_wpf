<UserControl x:Class="Electronics_store_db_wpf.MVVM.View.DocumentManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Electronics_store_db_wpf.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" >
            <Button Content="Товарная накладная" Margin="10"
                Height="50" Style="{StaticResource CustomButtonDarkStyle}"
                Command="{Binding CreateInvoiceCommand}"/>

            <Button Content="Отчет о клиентах" Margin="10"
                Height="50" Style="{StaticResource CustomButtonDarkStyle}"
                Command="{Binding CreateCustomerReportCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
